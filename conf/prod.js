// Configuration de production (ce que l'on livre)
var config = {
    css: {
        task: 'css:prod',
        name: 'styles.min.css',
    },
    path: this.destination + '/assets/css/',
};

module.exports = config;